{
  "devDependencies": {
    "cross-env": "^10.1.0",
    "jest": "^30.2.0",
    "mongodb-memory-server": "^10.2.2",
    "supertest": "^7.1.4"
  },
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watchAll"
  }
}
